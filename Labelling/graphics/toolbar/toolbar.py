import tkinter as tk

from Labelling.common.unimplemented import build_warning

def config_tool_bar(app):
    r = []

    # Modes

    # Line + Point Colour + Size



class ToolBar:
    def __init__(self, app):
        self.app = app
        self.tool_bar = config_tool_bar(app)